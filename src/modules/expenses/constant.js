export const MODULE_KEY = 'EXPENSES';
export const MODULE_NAME = 'EXPENSES';

export const LIST = `${MODULE_KEY}/list`;
export const SELECT = `${MODULE_KEY}/select`;
export const INCLUDE = `${MODULE_KEY}/include`;
export const EXCLUDE = `${MODULE_KEY}/exclude`;

export const INIT_STATE = {
	list: [],
	selected: {}
}